# ============================================================================
# Archiverr Dependencies
# ============================================================================
# This file contains both CORE dependencies (required for basic functionality)
# and OPTIONAL dependencies (for future features or development).
#
# Sync with setup.py: Core dependencies should match install_requires in setup.py
# ============================================================================

# ----------------------------------------------------------------------------
# CORE DEPENDENCIES (Required for production)
# ----------------------------------------------------------------------------

# HTTP client for API requests (TMDb, TVDb, TVMaze, OMDb)
requests>=2.31.0,<3.0.0

# YAML configuration parsing (config.yml)
pyyaml>=6.0.1,<7.0.0

# Jinja2 template engine (task system)
jinja2>=3.1.2

# Data validation (plugin SDK manifests, API schemas)
pydantic>=2.4.0,<3.0.0

# ----------------------------------------------------------------------------
# OPTIONAL DEPENDENCIES (Not required for basic functionality)
# ----------------------------------------------------------------------------

# Config validation (optional: schema validation)
# If not installed, validation is skipped with warning
jsonschema>=4.0.0

# Advanced regex support (optional: complex parsing patterns)
# Standard library 're' module is sufficient for most cases
regex>=2023.10.3

# Environment variable loading (optional: .env file support)
# Config.yml is the primary configuration method
python-dotenv>=1.0.0,<2.0.0

# ----------------------------------------------------------------------------
# DATABASE DEPENDENCIES
# ----------------------------------------------------------------------------

# PyMongo MongoDB driver (sync + async support)
# PyMongo 4.10+ includes AsyncMongoClient which replaces Motor
# Motor was deprecated in May 2025, support ends May 2027
pymongo>=4.10.0,<5.0.0

# DEPRECATED: Motor async driver
# Motor was deprecated by MongoDB in May 2025
# Use PyMongo's AsyncMongoClient instead (20-140% faster)
# motor>=3.3.0,<4.0.0  # DEPRECATED - do not use

# ----------------------------------------------------------------------------
# FUTURE FEATURES (Planned but not yet implemented)
# ----------------------------------------------------------------------------

# Task queue for async background execution (replaces subprocess)
taskiq>=0.11.0,<1.0.0
taskiq-mongodb>=1.0.0,<2.0.0

# Database ORM (planned: relational DB support)
sqlalchemy>=2.0.0,<3.0.0

# Web framework (planned: REST API backend)
fastapi>=0.104.0,<1.0.0

# ASGI server (planned: web UI hosting)
uvicorn[standard]>=0.24.0,<1.0.0

# MOVED TO CORE: pydantic is now required for plugin SDK
# pydantic>=2.4.0,<3.0.0  # Now in CORE DEPENDENCIES

# HTTP client for API testing
httpx>=0.25.0,<1.0.0

# ----------------------------------------------------------------------------
# TESTING DEPENDENCIES
# ----------------------------------------------------------------------------

# Testing framework
pytest>=7.4.0,<8.0.0

# Async testing support
pytest-asyncio>=0.21.0,<1.0.0

# Test coverage
pytest-cov>=4.1.0,<5.0.0

# Mock MongoDB for testing
mongomock>=4.1.0,<5.0.0